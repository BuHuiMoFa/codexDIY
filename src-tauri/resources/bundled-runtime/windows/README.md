Place staged offline runtime files here before building the installer.

Expected layout:
- `npm-global/`
- `git/` (optional but recommended on Windows)
- `manifest.json` (generated by the staging script)
